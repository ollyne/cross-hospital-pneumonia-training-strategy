# Preprocessing Artifacts

This folder documents the preprocessing artifacts used in the experiment.

The file `guangzhou_preprocessed.npz` is not uploaded directly to this repository due to file size limitations. The file size is approximately 102 MB, so the artifact is stored externally in the project storage folder.

## Artifact

```text
guangzhou_preprocessed.npz
```

## Description

This artifact contains the preprocessed Guangzhou dataset used for external-domain evaluation.

## Storage

The artifact can be accessed through the project storage folder:

```text
https://drive.google.com/file/d/1GdUyebdf8jEeJx6XVV_msDoNltg5jndV/view?usp=sharing
```

## Notes

The raw and preprocessed dataset files are excluded from this repository to keep the repository lightweight and easier to clone.
